{
  "scripts": {
    "start": "craco start",
    "build": "BUILD_PATH=../../dist/apps/nfid-frontend/ npx craco build",
    "build:example": "EXAMPLE_BUILD=1 BUILD_PATH=../../dist/apps/nfid-frontend/ craco build",
    "craco:test": "craco test",
    "test:dev": "npx jest --passWithNoTests --watch --detectOpenHandles",
    "test:dev:silent": "yarn test:dev --silent=true",
    "test": "npx jest --passWithNoTests --silent=true",
    "eject": "react-scripts eject",
    "serve": "npm start"
  },
  "browserslist": {
    "production": [
      "last 2 chrome version",
      "last 2 firefox version",
      "last 2 safari version",
      "last 2 edge version"
    ],
    "development": [
      "last 2 chrome version",
      "last 2 firefox version",
      "last 2 safari version",
      "last 2 edge version"
    ]
  },
  "repository": "git@github.com:internet-identity-labs/nfid-wallet-client.git",
  "license": "MIT"
}
