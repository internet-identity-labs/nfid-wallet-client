import React from "react"
import { CopyDevices } from "."

export const CONSTANTS = {
  base: "copy-devices",
}

export const CopyDevicesRoutes = {
  path: CONSTANTS.base,
  element: <CopyDevices />,
}
