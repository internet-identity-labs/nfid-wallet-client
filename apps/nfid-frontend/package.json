{
  "scripts": {
    "start": "craco start",
    "build": "BUILD_PATH=../../build/ craco build",
    "craco:test": "craco test",
    "test:dev": "jest --passWithNoTests --detectOpenHandles --watch",
    "test:dev:silent": "yarn test:dev --silent=true",
    "test": "jest --passWithNoTests --detectOpenHandles --silent=true",
    "eject": "react-scripts eject",
    "serve": "npm start"
  }
}
