import { nfidServiceWorkerNfidServiceWorker } from "./nfid-service-worker-nfid-service-worker"

describe("nfidServiceWorkerNfidServiceWorker", () => {
  it("should work", () => {
    expect(nfidServiceWorkerNfidServiceWorker()).toEqual(
      "nfid-service-worker-nfid-service-worker",
    )
  })
})
