export enum SwapStage {
  TransferSwap,
  Deposit,
  Swap,
  Withdraw,
  TransferNFID,
  Completed,
}

export enum SwapName {
  ICPSwap,
  Kongswap,
}

export enum PriceImpactStatus {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
}
