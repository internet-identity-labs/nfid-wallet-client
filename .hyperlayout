{
  "entry": "vertical",
  "layout": [
    [
      "dfx start",
      "cd src/ui-kit && yarn && yarn build:dev"
    ],
    [
      "yarn && yarn dev",
      "cd examples/MultipassDemo && yarn && yarn dev",
      "yarn tunnel"
    ]
  ]
}
