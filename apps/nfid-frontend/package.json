{
  "scripts": {
    "start": "craco start",
    "build": "BUILD_PATH=../../dist/apps/nfid-frontend/ craco build",
    "build:example": "EXAMPLE_BUILD=1 BUILD_PATH=../../dist/apps/nfid-frontend/ craco build",
    "craco:test": "craco test",
    "test:dev": "jest --passWithNoTests --watch --detectOpenHandles",
    "test:dev:silent": "yarn test:dev --silent=true",
    "test": "jest --passWithNoTests --silent=true",
    "eject": "react-scripts eject",
    "serve": "npm start"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ],
    "rules": {
      "jsx-a11y/alt-text": [
        0
      ]
    }
  },
  "browserslist": {
    "production": [
      "last 2 chrome version",
      "last 2 firefox version",
      "last 2 safari version",
      "last 2 edge version"
    ],
    "development": [
      "last 2 chrome version",
      "last 2 firefox version",
      "last 2 safari version",
      "last 2 edge version"
    ]
  },
  "license": "MIT",
  "dependencies": {
    "@slide-computer/signer": "^2.6.2"
  }
}
