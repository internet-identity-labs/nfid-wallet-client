{
  "scripts": {
    "start": "nx serve nfid-wallet-client",
    "build": "nx build nfid-wallet-client",
    "build:example": "EXAMPLE_BUILD=1 nx build nfid-wallet-client:example",
    "test:dev": "npx jest --passWithNoTests --watch --detectOpenHandles",
    "test:dev:silent": "yarn test:dev --silent=true",
    "test": "npx jest --passWithNoTests --silent=true",
    "serve": "npm start"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ]
  },
  "repository": "git@github.com:internet-identity-labs/nfid-wallet-client.git",
  "license": "MIT"
}
